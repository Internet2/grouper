<!-- Generate lib directory containing dependency jar files -->
<assembly>
  <id>lib</id>

  <formats>
    <format>dir</format>
  </formats>

  <!-- Override the base directory so that files get placed in base directory -->
  <baseDirectory></baseDirectory>

  <!-- Copy dependencies, including the project jar -->
  <dependencySets>
    <dependencySet>
      <outputDirectory>lib</outputDirectory>
      <useProjectArtifact>true</useProjectArtifact>
      <includes>
        <include>*:jar:*</include>
      </includes>
    </dependencySet>
  </dependencySets>
</assembly>
