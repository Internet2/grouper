<!-- $Id: build.xml,v 1.2 2004-12-03 15:06:28 blair Exp $ -->

<project name="csv2subject" default="build" basedir=".">

  <!-- set global properties for this build -->
  <property name="src"   location="."/>
  <property name="build" location="."/>

  <target name="build">
    <tstamp />
    <mkdir dir="${build}" />
    <javac srcdir="${src}" destdir="${build}"/>
  </target>

  <target name="clean">
    <delete>
      <fileset dir="${build}" includes="*.class"/>
    </delete>
  </target>

</project>

