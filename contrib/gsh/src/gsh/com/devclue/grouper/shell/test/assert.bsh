/*
 * For GrouperShell testing.
 * $Id: assert.bsh,v 1.1 2006-06-23 17:30:09 blair Exp $
 */
assert(String msg, boolean condition) {
  if (condition) {
    print("OK (" + msg + ")");  
  }
  else {
    print(
        "FAIL (" + msg + ") " 
      + "Line: "  + this.namespace.getInvocationLine()
      + " : "     + this.namespace.getInvocationText()
    );
    System.exit(1);
  }
}
