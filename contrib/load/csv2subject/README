
# Populate _grouper_subject_ table with I2MI Subjects

This code was contributed by The University Of Chicago.

$Id: README,v 1.5 2005-09-04 15:26:09 blair Exp $

---

## Build

    % cd $GROUPER_BASE/contrib/load/csv2subject
    % ant

--

## Configuration

_csv2subject_ uses Grouper's _conf/hibernate.properties_ file for its
JDBC configuration information.  No additional configuration should be
required.

--

## Run

    % cd $GROUPER_BASE/contrib/load/csv2subject
    % export CLASSPATH=.:/path/to/Grouper.jar:/path/to/Grouper-lib.jar:/path/to/grouper/conf/dir
    % java csv2subject -h
    % java csv2subject -f sample.csv

--

## Input File Format

Each line should be one of the following three formats.  A single .csv
file may combine all three formats.

  subjectID,subjectType
  subjectID,subjectType,name
  subjectID,subjectType,name,login

See the _sample.csv_ file for examples.

