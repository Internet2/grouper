# Grouper FAQ

$Id: FAQ,v 1.3 2004-12-04 19:03:10 blair Exp $

---

## General Questions

## Q: What's with the formatting?

A: The text version of this file is written using [Markdown
syntax](http://daringfireball.net/projects/markdown/).  I will be
adding a generated-HTML version of this document shortly.

---

## Using The API

### List Values

#### Q: How do I retrieve a _GrouperGroup_ object from a _GrouperList_ result?

A: Use the `GrouperList.group()` method.

    GrouperGroup g = (GrouperGroup) gl.group();

#### Q: How do I retrieve a _GrouperMember_ object from a _GrouperList_ result?

A: Use the `GrouperList.member()` method.

    GrouperMember m = (GrouperGroup) gl.member();

---

### Subjects

#### Q: How do I look up a subject of type _person_?

A: Use the `GrouperSubject.lookup(subjID, subjType)` method.

    Subject subj = GrouperSubject.lookup(subjID, Grouper.DEF_SUBJ_TYPE);

Or:

    Subject subj = GrouperSubject.lookup(subjID, "person");

The `GrouperSubject.lookup()` method will call the appropriate subject
type adapter for the type of subject specified.

#### Q: How do I look up a subject of type _group_?

A: Use the `GrouperSubject.lookup(subjID, subjType)` method.

    Subject subj = GrouperSubject.lookup(subjID, "group");

The `GrouperSubject.lookup()` method will call the appropriate subject
type adapter for the type of subject specified.

#### Q: How do I convert a _Subject_ object into a _GrouperMember_ object?

A: Use the `GrouperMember.lookup(subject)` method.

    Subject       subj  = GrouperSubject.lookup(subjID, subjType);
    GrouperMember m     = GrouperMember.lookup(subj);

