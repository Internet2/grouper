
# Grouper API 

This file contains a synopsis of the Grouper API.  For more details,
see the API javadoc and other documentation.

$Id: API,v 1.86 2005-07-28 19:12:47 blair Exp $

---

# Interfaces

## GrouperAccess

* boolean             can(privilege)
* boolean             grant(session, Group, GrouperMember, privilege)
* List                has(session, Group)
* List                has(session, GrouperMember, privilege)
* List                has(session, Group, GrouperMember)
* boolean             has(session, Group, GrouperMember, privilege)
* boolean             has(session, Group, privilege)
* List                has(session, privilege)
* boolean             revoke(session, Group, GrouperMember, privilege)
* boolean             revoke(session, Group, privilege)
* List                whoHas(session, Group, privilege)

---

## GrouperNaming

* boolean             can(privilege)
* boolean             grant(session, GrouperStem, GrouperMember, privilege)
* List                has(session, GrouperStem)
* List                has(session, GrouperMember, privilege)
* List                has(session, GrouperStem, GrouperMember)
* boolean             has(session, GrouperStem, GrouperMember, privilege)
* boolean             has(session, GrouperStem, privilege)
* List                has(session, privilege)
* boolean             revoke(session, GrouperStem, GrouperMember, privilege)
* boolean             revoke(session, GrouperStem, privilege)
* List                whoHas(session, GrouperStem, privilege)

---

# Classes

## Grouper

* String              static config(parameter)
* boolean             static groupField(groupType, groupField)
* List                static groupFields()
* boolean             static groupType(groupType)
* List                static groupTypeDefs()
* List                static groupTypes()

---

## GrouperAttribute

* String              field()
* String              value()

---

## GrouperField

* String              field()
* boolean             isList()
* String              readPriv()
* String              writePriv()

---

## GrouperGroup

* GrouperAttribute    attribute(attribute)
* void                attribute(attribute, value)
* Map                 attributes()
* GrouperGroup        static create(session, stem, extension)
* GrouperGroup        static create(session, stem, extension, groupType)
* String              createSource()
* Subject             createSubject()
* Date                createTime()
* void                static delete(session, Group)
* String              getDisplayExtension()
* String              getDisplayName()
* String              getExtension()
* List                getMembers()
* String              getName()
* String              getStem()
* String              static groupName(stem, extension)
* boolean             hasMember(GrouperMember)
* boolean             hasMember(GrouperMember, list)
* String              id()
* boolean             listAddVal(GrouperMember)
* boolean             listAddVal(GrouperMember, list)
* boolean             listDelVal(GrouperMember)
* boolean             listDelVal(GrouperMember, list)
* List                listEffVals()
* List                listEffVals(list)
* List                listImmVals()
* List                listImmVals(list)
* List                listVals()
* List                listVals(list)
* GrouperGroup        static load(session, stem, extension)
* GrouperGroup        static load(session, stem, extension, groupType)
* GrouperGroup        static loadByID(session, groupID)
* Group               static loadByName(session, name)
* Group               static loadByName(session, name, groupType)
* String              modifySource()
* Subject             modifySubject()
* Date                modifyTime()
* GrouperMember       toMember()
* String              type()

---

## GrouperList

* List                chain()
* Group               group()
* String              groupField()
* GrouperMember       member()
* Group               via()

---

## GrouperMember

* boolean             isMember(Group)
* boolean             isMember(Group, list)
* List                listEffVals()
* List                listEffVals(list)
* List                listImmVals()
* List                listImmVals(list)
* List                listVals()
* List                listVals(list)
* GrouperMember       static load(session, memberID)
* GrouperMember       static load(session, subjectID, subjectTypeID)
* GrouperMember       static load(session, subject)
* String              memberID()
* String              source()
* String              subjectID()
* Group               toGroup()
* String              typeID()

---

## GrouperQuery

* boolean             base(namespace)
* void                clear()
* boolean             clear(filter)
* boolean             createdAfter(date)
* boolean             createdBefore(date)
* List                getGroups()
* List                getListValues()
* List                getMembers()
* List                getStems()
* boolean             group(name)
* boolean             group(namespace, name)
* boolean             groupAttr(attribute, value)
* boolean             groupAttr(namespace, attribute, value)
* boolean             groupType(groupType)
* boolean             membershipType(membershipType)
* boolean             modifedAfter(date)
* boolean             modifiedBefore(date)
* boolean             stem(name)
* boolean             stem(namespace, name)
* boolean             stemAttr(name, value)
* boolean             stemAttr(namespace, name, value)

---

## GrouperSchema

* String              type()

---

## GrouperSession

* GrouperAccess       access()
* GrouperMember       getMember()
* GrouperNaming       naming()
* GrouperSession      static start(subject)
* boolean             stop()
* Subject             subject()

---

## GrouperStem

* GrouperAttribute    attribute(attribute)
* void                attribute(attribute, value)
* Map                 attributes()
* GrouperStem         static create(session, extension)
* GrouperStem         static create(session, stem, extension)
* String              createSource()
* Subject             createSubject()
* Date                createTime()
* void                static delete(session, Group)
* String              getDisplayExtension()
* String              getDisplayName()
* String              getExtension()
* List                getMembers()
* String              getName()
* List                static getRootStems(session)
* String              getStem()
* String              static groupName(stem, extension)
* List                groups()
* boolean             hasMember(GrouperMember)
* boolean             hasMember(GrouperMember, list)
* String              id()
* boolean             listAddVal(GrouperMember)
* boolean             listAddVal(GrouperMember, list)
* boolean             listDelVal(GrouperMember)
* boolean             listDelVal(GrouperMember, list)
* List                listEffVals()
* List                listEffVals(list)
* List                listImmVals()
* List                listImmVals(list)
* List                listVals()
* List                listVals(list)
* GrouperStem         static load(session, extension)
* GrouperStem         static load(session, stem, extension)
* GrouperStem         static loadByID(session, groupID)
* GrouperStem         static loadByName(session, name)
* String              modifySource()
* Subject             modifySubject()
* Date                modifyTime()
* List                stems()
* String              type()

---

## GrouperTypeDef

* String              field();
* String              type();

---

## MemberOf

* List                memberOf(GrouperList)

---

## MemberVia

* GrouperList         toList(session)

---

## SubjectFactory

* Subject             static getSubject(id)
* Subject             static getSubject(id, type)
* Subject             static getSubjectByIdentifier(id)
* Subject             static getSubjectByIdentifier(id, type)
* boolean             static hasType(type)
* Set                 static search(query)
* Set                 static types()


