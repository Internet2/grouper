
# Grouper API 

This file contains a synopsis of the Grouper API.  For more details,
see the API javadoc and other documentation.

$Id: API,v 1.57 2005-03-19 20:26:32 blair Exp $

---

## Interfaces

### GrouperAccess

#### Instance Methods

* boolean             can(privilege)
* boolean             grant(session, group, member, privilege)
* List                has(session, group)
* List                has(session, group, member)
* boolean             has(session, group, member, privilege)
* boolean             has(session, group, privilege)
* List                has(session, member, privilege)
* List                has(session, privilege)
* boolean             revoke(session, group, member, privilege)
* boolean             revoke(session, group, privilege)
* List                whoHas(session, group, privilege)

### GrouperNaming

#### Instance Methods

* boolean             can(privilege)
* boolean             grant(session, group, member, privilege)
* List                has(session, group)
* List                has(session, group, member)
* boolean             has(session, group, member, privilege)
* boolean             has(session, group, privilege)
* List                has(session, member, privilege)
* List                has(session, privilege)
* boolean             revoke(session, group, member, privilege)
* boolean             revoke(session, group, privilege)
* List                whoHas(session, group, privilege)

---

## Exceptions

### GrouperException

---

## Classes

### Grouper

#### Class Methods

* String              config(parameter)
* boolean             groupField(groupType, groupField)
* List                groupFields()
* boolean             groupType(groupType)
* List                groupTypeDefs()
* List                groupTypes()
* boolean             hasSubjectType(subjectTypeID)
* SubjectType         subjectType(subjectTypeID)
* List                subjectTypes()

### GrouperAccessImpl

#### Instance Methods

* boolean             can(privilege)
* boolean             grant(session, group, member, privilege)
* List                has(session, group)
* List                has(session, group, member)
* boolean             has(session, group, member, privilege)
* boolean             has(session, group, privilege)
* List                has(session, member, privilege)
* List                has(session, privilege)
* boolean             revoke(session, group, member, privilege)
* boolean             revoke(session, group, privilege)
* List                whoHas(session, group, privilege)

### GrouperAttribute

#### Instance Methods

* String              field()
* String              value()


### GrouperField

#### Instance Methods

* String              groupField()
* String              isList()
* String              readPriv()
* String              writePriv()


### GrouperGroup

#### Class Methods

* GrouperGroup        create(session, stem, extension)
* GrouperGroup        create(session, stem, extension, groupType)
* boolean             delete(session, group)
* String              groupName(stem, extension)
* GrouperGroup        load(session, stem, extension)
* GrouperGroup        load(session, stem, extension, groupType)
* GrouperGroup        loadByID(session, groupID)
* GrouperGroup        loadByID(session, groupID, groupType)
* GrouperGroup        loadByName(session, name)
* GrouperGroup        loadByName(session, name, groupType)

#### Instance Methods

* GrouperAttribute    attribute(attribute)
* boolean             attribute(attribute, value)
* Map                 attributes()
* String              createSource()
* String              createSubject()
* Date                createTime()
* String              id()
* boolean             listAddVal(member)
* boolean             listAddVal(member, listType)
* boolean             listDelVal(member)
* boolean             listDelVal(member, listType)
* List                listEffVals()
* List                listEffVals(listType)
* List                listImmVals()
* List                listImmVals(listType)
* List                listVals()
* List                listVals(listType)
* String              modifySource()
* Subject             modifySubject()
* Date                modifyTime()
* String              name()
* GrouperMember       toMember()
* String              type()

### GrouperList

#### Instance Methods

* GrouperGroup        group()
* String              groupField()
* GrouperMember       member()
* GrouperGroup        via()

### GrouperMember

#### Class Methods

* GrouperMember       load(session, subjectID, subjectTypeID)
* GrouperMember       load(session, subject)
* GrouperMember       load(session, memberID)

#### Instance Methods

* List                listEffVals()
* List                listEffVals()
* List                listImmVals(listType)
* List                listImmVals(listType)
* List                listVals()
* List                listVals(listType)
* String              memberID()
* String              subjectID()
* GrouperGroup        toGroup()
* String              typeID()

### GrouperNamingImpl

#### Instance Methods

* boolean             can(privilege)
* boolean             grant(session, group, member, privilege)
* List                has(session, group)
* List                has(session, group, member)
* boolean             has(session, group, member, privilege)
* boolean             has(session, group, privilege)
* List                has(session, member, privilege)
* List                has(session, privilege)
* boolean             revoke(session, group, member, privilege)
* boolean             revoke(session, group, privilege)
* List                whoHas(session, group, privilege)

### GrouperQuery

#### Instance Methods

* void                clear()
* boolean             clear(filter)
* boolean             createdAfter(date)
* boolean             createdBefore(date)
* boolean             groupType(groupType)
* boolean             membershipType(membershipType)
* boolean             modifedAfter(date)
* boolean             modifiedBefore(date)
* List                query()

### GrouperSchema

#### Instance Methods

* String              type()

### GrouperSession

#### Class Methods

* GrouperSession      start(subject)

#### Instance Methods

* GrouperAccess       access()
* boolean             stop()
* GrouperNaming       naming()
* Subject             subject()

### GrouperSubject

#### Class Methods

* Subject             load(subjectID)
* Subject             load(subjectID, subjectTypeID)

### GrouperSubjectAttribute

#### Instance Methods

* String              name()
* String              searchValue()
* String              typeID()
* String              value()

### GrouperTypeDef

#### Instance Methods

* FIXME String              groupField()
* FIXME String              groupType()

### GrouperUUID

#### Class Methods

* GrouperUUID         GrouperUUID()

### MemberOf

### Class Methods

* MemberOf            MemberOf(GrouperSession)

### Instance Methods

* List                memberOf(group, member, list)

### SubjectImpl

#### Instance Methods

* void                addAttribute(attribute, value )
* String[]            getAttributeArray(attribute)
* String              getDescription()
* String              getId()
* String              getName()
* SubjectType         getSubjectType()

### SubjectTypeAdapterGroupImpl

#### Instance Methods

* void                destroy()
* Subject             getSubject(subjectType, subjectID)
* Subject             getSubjectByDisplayId(subjectType, displayID)
* Subject[]           getSubjects(subjectType)
* void                init()
* boolean             isModifiable()
* Subject             newSubject(subjectType, subjectID, name, description, displayID)
* Subject             quickSearch(searchValue)
* Subject[]           searchByIdentifier(subjectType, subjectID)

### SubjectTypeAdapterPersonImpl

#### Instance Methods

* void                destroy()
* Subject             getSubject(subjectType, subjectID)
* Subject             getSubjectByDisplayId(subjectType, displayID)
* Subject[]           getSubjects(subjectType)
* void                init()
* boolean             isModifiable()
* Subject             newSubject(subjectType, subjectID, name, description, displayID)
* Subject             quickSearch(searchValue)
* Subject[]           searchByIdentifier(subjectType, subjectID)

### SubjectTypeImpl

#### Instance Methods

* SubjectTypeAdapter  getAdapter()
* String              getId()
* String              getName()







