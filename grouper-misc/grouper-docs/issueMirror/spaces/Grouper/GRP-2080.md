---
key: GRP-2080
cloud_key: 
onprem_url: https://todos.internet2.edu/browse/GRP-2080
type: Bug
status: Resolved
resolution: Fixed
priority: Critical
reporter: Michael Gettes <gettes@example.com>
assignee: Shilen Patel <shilen@example.com>
created: 2019-03-27T01:53:46.148+0000
updated: 2020-01-24T13:50:39.531+0000
resolved: 2020-01-24T13:50:39.508+0000
components: [grouperLoader]
fixVersions: [2.5.0]
labels: []
links: []
---

# GRP-2080  MAINTENACE_cleanLogs doesn't complete on MySQL server

MAINTENANCE_cleanLogs with > 2M rows wasn't completing in less than 300 sec and MySQL server was killing the query provided by Shilen in TIER grouper chat room.  Chat room follows:

Michael Gettes [21:47]
@Shilen Patel i couldn’t get the cleanLogs job to run (see https://internet2.slack.com/archives/C7V0UQDJ4/p1553515725317200) in less than 300 seconds; mySQL would keep killing it.  after staring at the SQL for a bit, i realized grouper_attribute_assign.attribute_assign_type in the subquery wasn’t indexed.  I indexed it and it ran in under a second - the cleanLogs job now runs completely.
Shilen Patel
This is the query, does it work for you?  SELECT distinct gaa.*, gadn.*, gad.* FROM grouper_attribute_assign gaa,  grouper_attribute_def_name gadn, grouper_attribute_def gad
WHERE  gaa.attribute_def_name_id = gadn.id AND gadn.attribute_def_id = gad.id AND gad.multi_assignable = 'F'
AND EXISTS (SELECT 1 FROM grouper_attribute_assign gaa2  WHERE gaa2.id != gaa.id AND gaa2.attribute_assign_action_id = gaa.attribute_assign_action_id
AND gaa2.attribute_def_name_id = gaa.attribute_def_name_id AND gaa2.attribute_assign_type = gaa.attribute_assign_type
AND gaa2.attribute_assign_type = 'stem' AND gaa2.owner_stem_id = gaa.owner_stem_id)
Posted in #tier-grouperYesterday at 08:08View message

## Comments

### Michael Gettes - 2019-03-29T13:32:30.720+0000

I recreated my DB and I have affirmed the index is not created on initialization.

### black.123 - 2019-04-06T01:44:24.361+0000

Can someone define the index ( example sql to add the index for MYSQL ) for:

    "i realized grouper_attribute_assign.attribute_assign_type in the subquery wasn’t indexed. I indexed it"

It would be great to fix this in a patch, but DDL operations are generally not done in a Grouper patch. ( And I kind of like that as a feature. :) ) Reserving DDL changes to "version upgrades" seems like a very wise thing to generally do.

### Michael Gettes - 2019-04-06T03:28:09.022+0000

I am on MySQL 8 (percona).  DDL adds:
 KEY `idx_grouper_attribute_assign_attribute_assign_type` (`attribute_assign_type`),

Have not had a chance to pursue the issue further of needed indices.  Gotta get taxes done first.

I hope this helps.

### Michael Gettes - 2019-04-07T16:59:42.102+0000

I have also found grouper_composites.type needs indexing

For MySQL:

The findbadmemberships query runs for forever.
A slight change makes it run in about 30 seconds on MySQL

select distinct c.factorOwnerUuid, c.uuid, m.uuid 
from MembershipEntry ms, Member m, Composite c 
where c.typeDb = 'intersection' 
and c.leftFactorUuid = ms.ownerGroupId and ms.fieldId = :fieldId and ms.enabledDb = 'T' and ms.memberUuid = m.uuid and m.subjectSourceIdDb <> 'g:gsa' 
and exists     
(select 1 from MembershipEntry ms2 
where ms2.ownerGroupId = c.rightFactorUuid      and ms2.memberUuid = m.uuid      and ms2.fieldId = :fieldId      and ms2.enabledDb = 'T') and not exists     
(select 1 from ImmediateMembershipEntry ms3 
where ms3.ownerGroupId = c.factorOwnerUuid      and ms3.memberUuid = m.uuid      and ms3.fieldId = :fieldId      and ms3.type = 'composite'      and ms3.enabledDb = 'T')
;

should be written as:

select distinct c.factorOwnerUuid, c.uuid, m.uuid 
from MembershipEntry ms, Member m, Composite c 
where c.typeDb = 'intersection' 
and c.leftFactorUuid = ms.ownerGroupId and ms.fieldId = :fieldId and ms.enabledDb = 'T' and ms.memberUuid = m.uuid and NOT m.subjectSourceIdDb = 'g:gsa' 
and 1 in      
(select 1 from MembershipEntry ms2 
where ms2.ownerGroupId = c.rightFactorUuid      and ms2.memberUuid = m.uuid      and ms2.fieldId = :fieldId      and ms2.enabledDb = 'T') and not 1 in     
(select 1 from ImmediateMembershipEntry ms3 
where ms3.ownerGroupId = c.factorOwnerUuid      and ms3.memberUuid = m.uuid      and ms3.fieldId = :fieldId      and ms3.type = 'composite'      and ms3.enabledDb = 'T')
;

exists is replaced with use of IN and NOT instead of <>

My grouperReport and findBadMemberships jobs do not complete and this query is the current blocker.  PLEASE can I get a fix for this if it works well for Oracle and others?

### Shilen Patel - 2019-05-02T13:19:08.801+0000

The two suggested index changes can be made in master once master is 2.5.  For now, I'll make this query update.  See https://todos.internet2.edu/browse/GRP-2117

### Shilen Patel - 2020-01-24T13:50:30.420+0000

Indexes added to master.

 

grouper_attribute_assign.attribute_assign_type

grouper_composites.type