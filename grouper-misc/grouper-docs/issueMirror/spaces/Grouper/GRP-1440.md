---
key: GRP-1440
cloud_key: 
onprem_url: https://todos.internet2.edu/browse/GRP-1440
type: Bug
status: Resolved
resolution: Fixed
priority: Minor
reporter: Chad Redman  (unc.edu) <chad_redman@example.com>
assignee: Chris Hyzer <mchyzer@example.com>
created: 2016-12-08T21:26:28.972+0000
updated: 2018-10-31T15:07:26.855+0000
resolved: 2018-10-31T15:07:26.799+0000
components: [UI]
fixVersions: [2.4.0]
labels: []
links: []
---

# GRP-1440  Usability of the Add Member dojo search field

1)
When entering IDs into the search field for adding users (labeled "Member name or ID"), entering text into the field will activate a spinning throbber on the field. While the cursor is in the text field the throbber never disappears, and there is no drop down of potential matches. However, if you tab out of the field or otherwise lose focus, the field changes to the display name of the found record.

The search field for adding users is set up as a dojo combo field, similar to the combo search field for groups. But unlike the group search, the user search does not work for partial search-as-you-type results. The dojo logic java class is invoked with a final wildcard on the query. But it then calls UiV2Group.lookup() to search, which looks up subjects by SubjectFinder.findByIdOrIdentifier(). Unless a source Id actually has a wildcard in the name, I think that nothing would be found.

As far as I can trace the code, it looks like when the field loses focus, it does the same query but without the wildcard. This would explain why moving from the field exposes the desired result.

I don't know what the fix should be. Dynamic search results for users would be nominally useful, although it would only be on the Id or Identifier (not a person's full name string, as the label would suggest). But wildcard queries might put a strain on LDAP servers or SQL databases. Maybe the optimal fix is to disable partial dynamic search results.

2) After input of a user's id, hitting enter instead of tab has inconsistent behavior depending on prior actions.

  - initially, it will change to the display name, but then flash an error "Select an entity from the search results"
  - continuing from above, if another id is entered, the result will be successful, *but it will be the previous ID that is added to the group*!
  - continuing from the previous entry, hitting return after a third entry will flash the error "Select an entity from the search results"

Suggested fix: remove carriage return functionality from the add member form

3)
The tooltip that shows up when the field is empty is "Enter 2 or more characters for searching". This would suggest that it is doing a partial search, which isn't functional per #1 above. The wildcard search also happens after the first character, without waiting for the minimal 2 characters.

4)
The label for the field is "Member name or ID". But the search is actually on Id or Identifier, not the name attribute or some variation on the user's display name.

Suggested fix: change base property groupSearchMemberOrId in grouperText/grouper.text.en.us.base.properties to "Member ID or unique identifier"


## Comments

### mchyzer - 2016-12-08T22:29:00.066+0000

Hmmm, lets discuss this one...

> 1)
> When entering IDs into the search field for adding users (labeled 
> "Member name or ID"), entering text into the field will activate a 
> spinning throbber on the field. While the cursor is in the text 
> field the throbber never disappears, and there is no drop down of 
> potential matches. However, if you tab out of the field or otherwise 
> lose focus, the field changes to the display name of the found record.

That’s not the experience I see.  I see a result pretty quickly.  Can you try the demo server?  I think your subject source is not answering the query quickly.  Can you try from GSH?

grouperSession = GrouperSession.startRootSession(); 
SubjectFinder.findAll(searchString, source);

Note, I think it should do at least prefix matching, if not substring matching.  Here is a screenshot from demo server

 

> 
> The search field for adding users is set up as a dojo combo field, 
> similar to the combo search field for groups. But unlike the group 
> search, the user search does not work for partial search-as-you-type 
> results. The dojo logic java class is invoked with a final wildcard 
> on the query. But it then calls UiV2Group.lookup() to search, which 
> looks up subjects by SubjectFinder.findByIdOrIdentifier(). Unless a 
> source Id actually has a wildcard in the name, I think that nothing 
> would be found.

I think the subject source should add wildcard

> 
> As far as I can trace the code, it looks like when the field loses 
> focus, it does the same query but without the wildcard. This would 
> explain why moving from the field exposes the desired result.

Right, losing focus will search by id or identifier, which is faster

> 
> I don't know what the fix should be. Dynamic search results for users 
> would be nominally useful, although it would only be on the Id or 
> Identifier (not a person's full name string, as the label would suggest). 
> But wildcard queries might put a strain on LDAP servers or SQL databases. 
> Maybe the optimal fix is to disable partial dynamic search results.

Hmmm, it would be nice to do a FAST search.  What subject source type do you use?  How many records are in it?

> 
> 2) After input of a user's id, hitting enter instead of tab has inconsistent 
> behavior depending on prior actions.
> 
>   - initially, it will change to the display name, but then flash an error 
>   "Select an entity from the search results"
>   - continuing from above, if another id is entered, the result will be 
>   successful, *but it will be the previous ID that is added to the group*!
>   - continuing from the previous entry, hitting return after a third entry 
>   will flash the error "Select an entity from the search results"
> 
> Suggested fix: remove carriage return functionality from the add member form

Hmmmm, I think enter key will work with id or identifier?  Or if you select a result from below?

Im trying to not edit the dojo combo control too much here…  we need faster queries


> 
> 3)
> The tooltip that shows up when the field is empty is "Enter 2 or more 
> characters for searching". This would suggest that it is doing a partial 
> search, which isn't functional per #1 above. The wildcard search also happens 
> after the first character, without waiting for the minimal 2 characters.

But the server will reject it and display the error message

> 
> 4)
> The label for the field is "Member name or ID". But the search is actually on 
> Id or Identifier, not the name attribute or some variation on the user's 
> display name.
> 
> Suggested fix: change base property groupSearchMemberOrId in 
> grouperText/grouper.text.en.us.base.properties to "Member ID or 
> unique identifier"

It is assumed your subject source searches by name.  If not you can override that label in your own grouper.text.en.us.properties if your subject source doesn’t support searching by name

Note, I would like to have a UI admin component available to test subject sources and answer some of these questions…  at some point… 

Anyways, let me know your thoughts
Thanks
Chris


### cer28 - 2016-12-10T19:19:40.714+0000

Hi Chris,

I discovered this in our own 2.3.0 candidate, but I verified it with the Unicom docker image, which has the same behavior. Interesting that both instances have only LDAP with no JDBC sources. I have been trying to trace it, but the debugger loses the jdpa connection during the LDAP call for some reason. Without the Java debugger, I can still see from network packets that it's only calling the LDAP ID search and identifier search, but not the general "search" search the way that JDBC sources do.

The Unicom setup should be fast, so it's not just a matter of performance. I'm going to keep digging in this, and hopefully can find out what's going on with the LDAP search.

Doing a wildcard search on the displayName field in LDAP is definitely going to do poorly against our LDAP servers. Assuming the base code isn't going to change, we would look into locally customizing, adding properties to tell dojo not to do wildcard searches for LDAP. We don't want to edit the JSP to take out dojo functions entirely, because resolving by identifier is still a useful function.

I'll also see if item #2 is reproducible with JDBC sources. It may be there, but hard to notice if the results are that quick. The carriage return does work to lose the focus and therefore do the identifier lookup, but the attempt to add the value seems to happen before it's resolved, thus causing the flash error message. If it's a real thing, displaying one subject but adding a different one would be a critical issue.

Thanks!




### cer28 - 2016-12-13T22:08:53.020+0000

Hi,

I understand the situation better now. I was incorrect that LDAP only did a subject search. It was that a search for a wildcarded id timed out after 2 minutes and I never waited that long.  I didn't expect that a search for a wildcarded identifer in LDAP would do so badly, but it does.

I have customized our DojoComboLogic class by being aware of  two new parameters in the grouper-ui properties file:

# dojo autocompletion for add member/add groups does a wildcard search
# for every character typed. If defined, only allow this for certain
# forms. If undefined, allow for all of them
uiV2.search.autocompleteById.allowedPaths = /UiV2Subject.addToGroupFilter, /UiV2Stem.createGroupParentFolderFilter
uiV2.search.autocompleteSearch.allowedPaths = /UiV2Subject.addToGroupFilter, /UiV2Stem.createGroupParentFolderFilter, /UiV2Group.addMemberFilter

I think a better way to customize this would have been to control it through a "DenyWildcardLookup" parameter in sources.xml, but I wanted to avoid changing core class like SourcesXmlResolver at this point.

The respective searches will still happen as long if there is no wildcard ending the term. So when you unfocus the field it still behaves the same. It's just the autocomplete that's turned off. I eventually let wildcards in the name search through, as it wasn't where the performance problems were. We did need to customize our name search template; the dojoComboLogic class removes the wildcard from the search term with the seeming assumption that  the search template will add them back. So we added one wildcard to our name search, and now it looks like this:

searchSubject
            <param-value>(&amp;(pid=%TERM%)(objectclass=UNCPerson))</param-value>

searchSubjectByIdentifier
            <param-value>(&amp;(|(uid=%TERM%)(eduPersonPrincipalName=%TERM%))(objectclass=UNCPerson))</param-value>

search
         <param>
            <param-name>filter</param-name>
            <param-value>(&amp;(|(uid=%TERM%)(pid=%TERM%)(displayName=%TERM%)(sn=%TERM%))(objectclass=UNCPerson))</param-value>
        </param>
         <param>
            <param-name>firstlastfilter</param-name>
            <param-value>(&amp;(|(sn=%LAST%)(uncPreferredSurname=%LAST%))(|(givenName=%FIRST%*)(eduPersonNickname=%FIRST%*))(objectClass=UNCPerson))</param-value>
        </param>

The net effect is that not much shows up until you (a) enter a full pid or uid; or (b) enter a comma after a surname, after which it starts to return a match list. It's probably the best tradeoff between UI usability and performance.


Item #2 (using carriage returns to submit without picking an entity from the dropdown, adds an earlier entry instead of the current one) is definitely there even in the quickstart. app. I'm going to look at changing the jsp to disable a submit on carriage return, which should be a quick fix for that.

-Chad




### mchyzer - 2016-12-14T16:05:38.499+0000

can we mark this as resolved?  do you need to change the logic for the search box in upper right of UI?

Thanks
Chris

### cer28 - 2016-12-14T16:09:45.229+0000

The upper right search is ok as far as I know. That's not using any autocompletion, right?

What about hitting carriage return on the combo field before it resolves? Have you been able to reproduce it?


### mchyzer - 2016-12-14T16:27:20.616+0000

the search in the upper right will do a search, not sure if you want to do something with wildcards there or not.

yes, enter key is a problem.  wonder if we can disable it on form with this in the correct grouper js file

{code}
$('#add-members-form').on('keyup keypress', function(e) {
  var keyCode = e.keyCode || e.which;
  if (keyCode === 13) { 
    e.preventDefault();
    return false;
  }
});
{code}

I will give this a try

### cer28 - 2016-12-15T17:57:13.621+0000

I tried out adding the same inner code as that, but in _TextBoxMixin->onInput(). That's a wide net, but seems to fix issues in add-members-form and add-groups-form. And it doesn't seem to hurt the stem field of the new group form, or any of the Lite fields, since the return key already didn't submit.

the add groups combo field had a few of the same problems as the add members field, but not as bad. You still get an error if you hit enter on a blank field or an invalid group. But if you have a valid group path it resolves it before submitting. That would be a different way of fixing the add member form, if I knew what magic made that happen :)

The folder combo in the new group form was already ok, as it didn't submit anything on return.


### mchyzer - 2016-12-15T21:11:34.332+0000

do you want to submit a pull request for that?  Is there a way to enable or disable that with a config properties?   i.e. the javascript would be written by the servlet if set to true (default to true)

thanks
Chris

### cer28 - 2016-12-19T14:15:02.747+0000

I couldn't think of an elegant way to control the JS output via properties. But I did take it out of the JS and put it into the groupHeader.jsp page. It seems very hackish but it's effective for us.




{code:java}
From 4dc63fa48930151bd029bb035ded8a8fa6dad7a6 Mon Sep 17 00:00:00 2001
From: Chad Redman <chad_redman@example.com>
Date: Sun, 18 Dec 2016 17:59:53 -0500
Subject: [PATCH] Trap carriage returns in the group Add Member text field --
 otherwise it submits before resolving the subject

---
 grouper-ui/webapp/WEB-INF/grouperUi2/group/groupHeader.jsp | 11 ++++++++++-
 1 file changed, 10 insertions(+), 1 deletion(-)

diff --git a/grouper-ui/webapp/WEB-INF/grouperUi2/group/groupHeader.jsp b/grouper-ui/webapp/WEB-INF/grouperUi2/group/groupHeader.jsp
index ba955f0..76867be 100644
--- a/grouper-ui/webapp/WEB-INF/grouperUi2/group/groupHeader.jsp
+++ b/grouper-ui/webapp/WEB-INF/grouperUi2/group/groupHeader.jsp
@@ -57,7 +57,16 @@
                                     <grouper:combobox2 idBase="groupAddMemberCombo" style="width: 30em"
                                       filterOperation="../app/UiV2Group.addMemberFilter?groupId=${grouperRequestContainer.groupContainer.guiGroup.group.id}"/>
                                     ${textContainer.text['groupSearchLabelPreComboLink']} <a href="#member-search" onclick="$('#addMemberResults').empty();" role="button" data-toggle="modal" style="text-decoration: underline !important;">${textContainer.text['groupSearchForEntityLink']}</a>
-                                    
+                                    <script>
+                                      $('#add-members-form').on('keyup keypress', function(e) {
+                                        var keyCode = e.keyCode || e.which;
+                                        if (keyCode === 13) {
+                                          e.preventDefault();
+                                          return false;
+                                        }
+                                      });
+                                    </script>
+
                                   </div>
                                 </div>
                               </div>
-- 
2.8.3


{code}


### cer28 - 2017-02-16T16:21:38.452+0000

(never mind, this comment was for GRP-1448 which was already resolved) ... We applied UI patch 11 on a dev server, and the carriage return fix looks good in quick testing.


### cer28 - 2017-03-23T14:46:37.892+0000

Spoke too soon -- UI patch 11 may have introduced new issue GRP-1489. Filtering carriage returns impacts not only text fields but textarea fields, which means multiple lines can't be entered in the Add Members via copy/paste textarea.

### cer28 - 2017-10-30T17:05:28.965+0000

Possibly resolved. The new properties for whether to use dojo wildcard use aren't in a 2.3 patch, but the code change is in the master branch. The other issue about carriage returns is fixed in GRP-1489 (UI 2.3 patch 30).