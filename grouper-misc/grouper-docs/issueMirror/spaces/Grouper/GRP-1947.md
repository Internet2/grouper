---
key: GRP-1947
cloud_key: 
onprem_url: https://todos.internet2.edu/browse/GRP-1947
type: Improvement
status: Resolved
resolution: Fixed
priority: Minor
reporter: Chad Redman  (unc.edu) <chad_redman@example.com>
assignee: Chad Redman  (unc.edu) <chad_redman@example.com>
created: 2018-11-08T18:34:32.630+0000
updated: 2021-12-04T22:22:24.981+0000
resolved: 2021-12-04T22:22:24.940+0000
components: [API]
fixVersions: [2.6.5]
labels: []
links: []
---

# GRP-1947  API Group.replaceMembers() doesn't work with privileges

Method Group.replaceMembers(Collection<Subject> newSubjectList, Field field) – you can pass in a Field, but the only field that works is members. If you pass in a readers or updaters field, it throws a SchemaException error, because internal_addMember() is specifically rejecting non-list types.

 

There is a web service that can replace privileges (WsRestAssignGrouperPrivilegesRequest), but no API equivalent. It would be nice to have an API method to sync privileges using the existing algorithms, rather than having to rewrite it in caller code.

 

## Comments

### Chad Redman  (unc.edu) - 2019-09-16T20:44:18.136+0000

Also applies to {code}Group.addMember(Subject subj, Field f, boolean exceptionIfAlreadyMember){code}. It rejects anything without a fieldType of LIST, and the only field of that type is member. So the field isn't exactly ignored, but doesn't do anything more than {code}addMember(Subject subj, boolean exceptionIfAlreadyMember){code} does.

I was hoping for a single method to copy memberships and privileges from one user to another. But it looks like I can't use this method, and need separate membership and privilege method calls.

### Chad Redman  (unc.edu) - 2019-09-16T20:54:44.456+0000

The addition of this LIST check was made in March 2007:

{noformat}
commit 4b82d28b52ee7783b280a40c6bcc93a3b7596fd8
Author: Blair Christensen <blair@example.com>
Date:   Mon Mar 5 20:04:17 2007 +0000

    killing off "GroupValidator" is taking much longer than i expected.


    git-svn-id: https://svn.internet2.edu/svn/i2mi/trunk@3666 bc24b658-cf39-4249-b827-e3526eaaeb42
{noformat}

Unfortunately no other explanation in the comments or commit on why this needed to be added.

### Chad Redman  (unc.edu) - 2019-09-16T20:58:27.144+0000

Annnd also applies to deleteMember(). This is the real reason why replaceMembers() doesn't work for privileges -- it works by calling addMember and deleteMember, and doesn't do any field type filtering on its own.

### Chris Hyzer - 2019-09-17T15:30:12.209+0000

are you going to make a method that you need on Group?

### Chad Redman  (unc.edu) - 2019-09-18T17:35:42.379+0000

Maybe can resolve this with three tasks:
 * add to the javadoc that this will only work for memberships and not privileges, so maybe use the method without the field parameter
 * create a new method something like add/deleteMemberOrPrivilege(member, field), that will take a member and field, and if the field is an access type, call grantPriv/revokePriv instead of addMember. This will need to map between fields and privileges, since grant/revokePriv works on Privilege, not Field
 * create a new method replaceMembersOrPrivileges(subjects, field) that calls the above new method instead of add/deleteMember

Is it wrong to be doing anything using fields coming from privileges? It's what MembershipFinder returns, so it would be nice if it were usable

### Chris Hyzer - 2019-09-18T18:17:36.196+0000

ugh something about that doesnt sit well with me either.

im recanting...  i think you can just make the field methods work for privs.  dont want to have api bloat.  ok?  :)

overall you want to copy memberships and privs from one user to another?  that would be a new method right?

### Chad Redman  (unc.edu) - 2021-04-16T05:53:39.157+0000

Since 2 years have gone by, I forgot about this JIRA, and had tried using replaceMembers() again to replace privileges.

Ideally, this would use privileges instead of fields, which require a FieldFinder to use. But if we can get this to work with fields as the method signature suggests it should, that would be good enough.

### Chad Redman  (unc.edu) - 2021-12-04T22:22:24.978+0000

I just commented out those lines restricting the field types. Seems to work fine with non-list access fields.