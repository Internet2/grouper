---
key: GRP-1651
cloud_key: 
onprem_url: https://todos.internet2.edu/browse/GRP-1651
type: Bug
status: Resolved
resolution: Fixed
priority: Minor
reporter: Chris Hyzer <mchyzer@example.com>
assignee: Chris Hyzer <mchyzer@example.com>
created: 2018-01-02T01:30:59.704+0000
updated: 2018-01-11T14:46:22.371+0000
resolved: 2018-01-02T01:35:16.192+0000
components: [UI]
fixVersions: [2.3.1, 2.4.0, 2.3.0.patch]
labels: []
links: []
---

# GRP-1651  inherited privileges should assign those privileges to subobjects

if you assign inherited privileges to stems / groups / attributes, those should immediately apply to current objects in addition to new objects.  Note, currently this will happen in a daemon in the background.

## Comments

### mchyzer - 2018-01-02T01:35:16.204+0000

This is fixed in git

If you assign inherited privileges in the Grouper UI it will immediate propagate to descendant objects.  If this takes longer than 20 seconds it will continue processing in a thread and tell the user it is happening.  If it takes less than 20 seconds the UI will let the user know it is done.

grouper.text.en.us.base.properties
{code}
# success removing rules for inherited privileges
stemPrivilegesInheritedAddSuccesses = Success: added ${grouperRequestContainer.stemContainer.successCount} inherited privilege entries
stemPrivilegesInheritedAddSuccessesNotDone = Success: added ${grouperRequestContainer.stemContainer.successCount} inherited privilege entries, but sub-object assignments are still being processed
{code}

UiV2Stem.privilegeInheritanceAddMemberSubmit line 2693
{code}
      // run the daemon so these privs bubble down to the sub objects
      final boolean[] DONE = new boolean[]{false};
      
      Thread thread = new Thread(new Runnable() {

        @Override
        public void run() {
          GrouperSession grouperSession = GrouperSession.startRootSession();
          try {
            
            RuleApi.runRulesForOwner(stem);
            
            DONE[0] = true;
          } catch (RuntimeException re) {
            LOG.error("Error in running daemon", re);
          } finally {
            GrouperSession.stopQuietly(grouperSession);
          }
          
        }
        
      });

      thread.start();
      
      try {
        thread.join(45000);
      } catch (Exception e) {
        throw new RuntimeException("Exception in thread");
      }

      if (DONE[0]) {

        guiResponseJs.addAction(GuiScreenAction.newMessage(GuiMessageType.success, 
            TextContainer.retrieveFromRequest().getText().get("stemPrivilegesInheritedAddSuccesses")));
        
      } else {

        guiResponseJs.addAction(GuiScreenAction.newMessage(GuiMessageType.success, 
            TextContainer.retrieveFromRequest().getText().get("stemPrivilegesInheritedAddSuccessesNotDone")));
      }
      

{code}

### mchyzer - 2018-01-02T02:18:46.952+0000



From: Akki Kumar [mailto:amazingquestions007@example.com] 
Sent: Friday, April 28, 2017 10:42 AM
To: Hyzer, Chris <mchyzer@example.com>; grouper-users@example.com
Subject: Remove Privileges from Groups

Hello,

I am running below commands to add privileges to a folder/groups. (Link: https://spaces.internet2.edu/display/Grouper/Grouper+rules+use+case+-+Inherited+privileges+on+groups)

grouperSession = GrouperSession.startRootSession();
stemToAssign = StemFinder.findByName(grouperSession, "Enterprise:Deparment:Information_Technology");
all = findSubject("etc:Admins");
RuleApi.inheritGroupPrivileges(SubjectFinder.findRootSubject(), stemToAssign, Stem.Scope.SUB, all, Privilege.getInstances("admin"));
RuleApi.runRulesForOwner(stemToAssign);

However, there is no documentation on how to remove inherited privileges from a folder/groups. Is there a Grouper API or GSH commands that I can run through GSH to remove privileges?  Any guidance is truly appreciated.

Thank you,
Akki
