---
key: GRP-5470
cloud_key: 
onprem_url: https://todos.internet2.edu/browse/GRP-5470
type: New Feature
status: Open
resolution: Unresolved
priority: Minor
reporter: Chris Hyzer <mchyzer@example.com>
assignee: Vivek Sachdeva <erviveksachdeva@example.com>
created: 2024-05-29T05:57:56.144+0000
updated: 2024-07-20T17:14:20.760+0000
resolved: 
components: []
fixVersions: []
labels: []
links: []
---

# GRP-5470  add support for multiple keys in JWT WS

Jeffrey Crawford
  25 days ago
We are starting to use JWT auth entities but I’ve got the following questions since I’m new at JWT
Are JWT private keys usually set to expire after some time? If so how long, I’m assuming a year
When rotating keys, can you generate a new private key, early to hand off to a client, while they still use the old key for authentication? (AKA no downtime rotation)
Is there a way to change the owner (who to email) after an JWT service account is created for the email job?
(edited)


Chris Hyzer
  25 days ago
Are JWT private keys usually set to expire after some time? If so how long, I’m assuming a year
When you create it, you say when you want it to expire
When rotating keys, can you generate a new private key, early to hand off to a client, while they still use the old key for authentication? (AKA no downtime rotation)
I believe when a local entity is ready to rotate they go to the ui and get a new private key and it removes the old, so they would need to have a short process to load that wherever it goes
Is there a way to change the owner (who to email) after an JWT service account is created for the email job?
Im not sure there is something easy there, would need a GSH template to set the new id.  we should probably add a way to associate a group with it instead
(edited)


Jeffrey Crawford
  19 days ago
I’m thinking the key rotation is the most pressing issue. Many systems are starting to rely on seamless operation. Could a future update keep the last key around to allow for our team to prepare a new key, while the previous one stays configured?
New


Alpha Sanneh
  1 day ago
I have also started testing this out and the most pressing issue is the key rotation and the current implementation does not allow for creating a new one whilst the old one is still in use which means once I generate a new private key the client needs to load that quickly to prevent any downtime.  For some of our shops that means a deployment so need to add that to our SLA once I roll it out. It would be useful to add a feature to allow for generating a new key with a future enable date to allow for seamless rotation from clients
:heavy_plus_sign:
1



Chris Hyzer
  3 minutes ago
yes we can do that
