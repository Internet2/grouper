---
key: GRP-2049
cloud_key: 
onprem_url: https://todos.internet2.edu/browse/GRP-2049
type: Bug
status: Resolved
resolution: Unresolved
priority: Minor
reporter: Chad Redman  (unc.edu) <chad_redman@example.com>
assignee: Chad Redman  (unc.edu) <chad_redman@example.com>
created: 2019-03-05T13:20:07.858+0000
updated: 2020-01-22T03:39:59.410+0000
resolved: 
components: [grouperInstaller]
fixVersions: []
labels: []
links: [duplicates GRP-1157]
---

# GRP-2049  Installer checks for "javac" multiple times, once incorrectly

The Grouper installer has two different ways of checking for valid java and javac executables, and one of them causes problems during my install. I have JAVA_HOME set to the jdk, but if I do an install using the jre java, I get error "This needs to be run from a jdk, but it is detected to be running from a JRE... run the installed from a JDK!". The installer shouldn't need to be _run_ from the jdk, it should just check whether javac can be found somewhere.

The first check is when the program starts, before the first prompt. It calls method validJava(), which does the following checks:
 * property java.version >= 1.7
 * System.getenv("JAVA_HOME") is set
 * $JAVA_HOME/bin/java -version shows version a.b.c and version >= 1.7
 * $JAVA_HOME/bin/javac -version shows version a.b.c. and version >= 1.7
 * "java -version" shows a.b.c and version >= 1.7
 * "javac -version" shows a.b.c and version >= 1.7

These all work successfully (Oracle jdk 1.8).

After choosing action "install", the installer follows the mainInstallLogic() method. Here it first asks for hsqldb information, then calls validJavaVersion(), which follows different logic from validJava():
 * System.getProperty("java.home") + /bin/java -version shows version a.b.c_d and version >= 1.6
 * The above java path, plus "c -version" (i.e. ...bin/java*c* -version) shows version a.b.c_d and version >= 1.6

A few problems here:

1) getProperty("java.home") is for the jre, and getenv("JAVA_HOME") is for the jdk. In my case, java.home is the jre java
 2) The installer has already checked JAVA_HOME before the first prompt, so there is no need to check it again
 3) The first check is for Java 1.7+, the second is for 1.6+
 4) It shouldn't be necessary to run the installer from the jdk, as long as javac can be found in the path
 5) If a second check is really needed, it should happen before all the hsql prompts so it can fail earlier

Possibly introduced with GRP-1425: admin task in grouperInstaller to help with log management .

## Comments

### cer28 - 2019-03-05T13:57:50.135+0000

Actually I'm not seeing any installer command that calls javac. Maybe this is a safety check before ant builds the UI? If so, ant gets the path using JAVA_HOME, so the first check in main() should be sufficient. Other than that, I don't why any installer command that directly executes java can't use the same jre that was used to run the installer.


### Chad Redman  (unc.edu) - 2019-08-25T03:16:26.615+0000

I have refactored this a lot. The initial checking hasn't changed much, except the warnings have improved. I also move the original java-home check (the java command running the installer) out of the installer check and into the initial section with the other checks.
{quote}Non-fatal ERROR: $JAVA_HOME/bin/java should be invoked with Java 1.7+, but was: 1.6.0
 Non-fatal ERROR: $JAVA_HOME/bin/javac requires to be invoked with Java 1.7+ JDK, but was: 1.6.0
 Non-fatal ERROR: java command in the PATH should be invoked with Java 1.7+, but was: 1.6.0
 Non-fatal ERROR: javac command in the PATH requires to be invoked with Java 1.7+ JDK, but was: 1.6.0
 Non-fatal ERROR: the current java command running the installer should be invoked with Java 1.7+, but was: 1.6.0
 WARNING: JAVA_HOME or Java path errors may cause issues when running external commands - these should be fixed before continuing.
 Press <enter> to continue...
{quote}
 It also does a real version check based on individual dotted components. It doesn't convert it to a decimal and compare numerically, which would not work with java 1.10+.

 

### Chad Redman  (unc.edu) - 2019-08-25T03:46:08.732+0000

Also refactored the version comparison for jar versions, which can now use the compareVersions() method, rather than using GiGrouperVersion. The latter works ok, but is specific for Grouper versions and not well suited for generic version comparisons. I also modified the regexp to handle any 2+ dotted components, not specifically 3 (e.g. a.b.c), since jar versions don't always have exactly three numbers in a version.

### Chad Redman  (unc.edu) - 2020-01-22T03:39:59.410+0000

The original branch was never merged to master. I did a rebase from current master,  refreshed the dates and then merged to master.

 