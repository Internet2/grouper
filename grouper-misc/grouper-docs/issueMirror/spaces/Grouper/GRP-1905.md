---
key: GRP-1905
cloud_key: 
onprem_url: https://todos.internet2.edu/browse/GRP-1905
type: Bug
status: Resolved
resolution: Fixed
priority: Major
reporter: Stefano Angioni <s.l.angioni@example.com>
assignee: Chad Redman <credman@example.com>
created: 2018-09-19T15:37:45.251+0000
updated: 2025-01-04T22:59:20.248+0000
resolved: 2024-12-02T02:29:08.384+0000
components: [API]
fixVersions: [4.17.0, 5.15.0]
labels: []
links: [is related to GRP-3817, is related to GRP-5790]
---

# GRP-1905  Cannot find groups having a particular attribute value

We are attempting to use GroupAttributeFilter to find groups having a particular attribute value. The code looks like this:

GrouperSession gs = <get session>
GroupAttributeFilter gaf = new GroupAttributeFilter(
     "bath:provisionClass", "groupmanager-groups",
     StemFinder.findRootStem(gs));
Set<Group> ggroups = gaf.getResults(gs);

No results are returned. This appears to be because in Hib3GropDAO.findAllByApproximateAttrHelper the following code...

String attributePrefix = legacyAttributeStemName + ":" + legacyAttributePrefix; if (attr.startsWith(attributePrefix)) \{
     byHql.setString("attributeName",  attr); } else \{
     byHql.setString("attributeName",  attributePrefix + attr); }

...is actually setting the name of the attribute being examined to:

etc:legacy:attribute:legacyAttribute_bath:provisionClass

Are we missing something, or is there a bug here?

We are using version 2.3.0 of the grouper Maven artifact, but this code seems unchanged in the latest updates to GitHub.

## Comments

### Chad Redman  (unc.edu) - 2019-08-07T01:19:32.768+0000

The GroupAttributeFilter class is indeed set up to look for legacy attributes. There was a migration of the API to deal with attributes at a more granular level, and at that time older style attributes were renamed to "etc:legacy:attribute:legacyAttribute" + the original name. Some of the existing API calls only handled the older format, and those methods automatically look for the legacy prefix.

What you want is the newer API for searching attributes. In this case it is
{code:java}
Set<Group> ggroups = new GroupFinder().assignNameOfAttributeDefName("bath:provisionClass").assignAttributeValue("groupmanager-groups").findGroups() {code}

I will leave this open; there may be a case for changing Hib3GroupDAO to work with non-legacy attributes, but it may just be a giant can of worms. Or, maybe add documentation, and/or deprecation notes for these methods. There is nothing on them now to indicate they are for legacy attributes.

### Chad Redman  (unc.edu) - 2019-08-07T01:27:48.415+0000

Note also: GroupFinder().findAllByAttribute also only works  for legacy attributes. Thus, GroupFinder is a mix of old and new defaults, which is even more confusing.

### Chad Redman - 2024-10-30T18:26:52.824+0000

Also affects WS calls with wsQueryFilter FIND_BY_EXACT_ATTRIBUTE. This calls Hib3GroupDAOfindAllByAttr(...) which only works for legacy attributes

### Chad Redman - 2024-12-02T02:29:08.425+0000

Fixed to work for any attribute, not just legacy attributes, while maintaining some legacy support. If the attribute name does not contain a colon (:), assume it's a legacy attribute and add the legacy prefix. Otherwise, use the name as is.