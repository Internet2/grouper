---
key: GRP-1931
cloud_key: 
onprem_url: https://todos.internet2.edu/browse/GRP-1931
type: Improvement
status: Resolved
resolution: Fixed
priority: Minor
reporter: Bert Bee-Lindgren <bert.bee-lindgren@example.com>
assignee: Bert Bee-Lindgren <bert.bee-lindgren@example.com>
created: 2018-10-16T13:31:18.370+0000
updated: 2019-03-18T11:00:12.140+0000
resolved: 2019-03-18T10:28:10.194+0000
components: [provisioning]
fixVersions: [2.4.0.patch]
labels: []
links: []
---

# GRP-1931  PSPNG - Slow FullSyncs

From University of Auckland (auckland.ac.nz)...

Our PSPNG full sync is very very slow.
 For 6321 groups (150656 memberships), our full sync runs for 50 mins.
 In total, we have 150k+ groups (18mil memberships) which can potentially run a full sync for 2+ days. This is contrasting to the PSPNG statement,
 If grouper-loader.properties has a full-sync schedule, then all the grouper groups which are enabled for provisioning to a target will be queued for reconciliation at the start of that schedule.
 This only takes a short while because the reconciliations occur asynchronously.
 Are there other members of the community you know that uses PSPNG for these many memberships? If yes, how long does it takes for them to run a full sync and how did they optimise it at that speed?

## Comments

### bert - 2018-10-16T13:32:23.565+0000

I'd like to also learn more from your experiences. I'm wondering where the time is being spent, so I'm thinking about instrumenting the time taken in reading from Grouper DB, Groups from LDAP, Subjects from LDAP.
 
In thinking about this and looking at the code, I'm thinking that maybe subject-reading is happening over and over as each full-sync flushes the cache to be safe; perhaps it would help a lot if a FullSync sweep shared a cache.
 
I'll patch for instrumentation and then see if subject-reading or something else needs to be optimized.

### bert - 2019-03-18T10:28:10.208+0000

FullSync performance should be much faster with Grouper 2.4, PSPNG Patch #4

 

### bert - 2019-03-18T11:00:12.140+0000

Here are some more details about how performance was improved (copied from grouper-users list):

 

- Before this patch, PSPNG FullSyncs always started fresh (no caching) to make sure all the data was up to date. This meant that a Full-Sync-All-Groups operation would repeatedly fetch Subject information over and over

 

- This patch tracks the timestamp of FullSync requests and cached information, enabling cached information to be reused whenever it is newer than the FullSync request.

 

- Because extra checks are now in place for stale caching, the default cache times have been increased from 10 minutes to 12 hours. This can be adjusted with the dataCacheTime_secs configuration property

 

- Multiple LDAP connections can be used to fetch Subject information more quickly with, of course, more load on the LDAP server. The number of LDAP-fetching workers can be adjusted with numberOfDataFetchingWorkers, which defaults to 1.

 

- To encourage properly sized caches, warnings are issued when caches get too full. These warnings can be disabled with areCacheSizeWarningsEnabled (defaults to true) and cacheFullnessWarningThreshold_percentage (defaults to 95).

 

- When subjects are usually expected to be in the PSPNG-Provisioned system, it can be helpful to warn when they are not found for some reason. missingSubjectsWarningThreshold_percentage can be set to a percentage below which information about missing subjects will be logged. this defaults to 100% so such information is never logged.