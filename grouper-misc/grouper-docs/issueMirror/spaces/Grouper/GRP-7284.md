---
key: GRP-7284
cloud_key: 
onprem_url: 
type: Improvement
status: To Do
resolution: Unresolved
priority: Medium
reporter: Chris Hyzer <mchyzer@example.com>
assignee: Vivek <?>
created: 2026-08-30T19:00:55.466-0400
updated: 2026-08-30T19:00:55.654-0400
resolved: 
components: []
fixVersions: []
labels: []
links: []
---

# GRP-7284  GRP-7178 used lambdas as Runnable arguments in GrouperHooksUtils; replace with plain try/catch per hook

GRP-7178 introduced eight lambdas passed as Runnable arguments in GrouperHooksUtils.registerHooksIfNecessary, e.g.:

{code:java}registerBuiltInHook("GroupDoNotDeleteIfProvisionable",
    () -> GroupDoNotDeleteIfProvisionable.registerHookIfNecessary());{code}

The Grouper coding guidelines (CLAUDE.md in the repo root) say to avoid lambdas used as functional arguments, because they produce weak stack traces and awkward breakpoint behaviour. That matters more than usual here: the whole purpose of this code is reporting which built in hook failed to register, and a lambda turns the useful frame into a synthetic lambda$registerHooksIfNecessary$6.

Suggested fix - drop the functional type entirely rather than swapping in an anonymous inner class. No Runnable, no deferred call, and the stack trace points at the real call site. Replace the eight registerBuiltInHook(...) calls with:

{code:java}          // each built in hook registers in its own try/catch.  If one throws (generally a
          // grouper.properties misconfiguration) log it loudly and keep going, otherwise every
          // hook after it in this sequence is silently skipped for the life of the jvm.
          try {
            AttributeDefAttributeNameValidationHook.registerHookIfNecessary(true);
          } catch (RuntimeException re) {
            logBuiltInHookRegistrationError("AttributeDefAttributeNameValidationHook", re);
          }
          try {
            AttributeDefNameAttributeNameValidationHook.registerHookIfNecessary(true);
          } catch (RuntimeException re) {
            logBuiltInHookRegistrationError("AttributeDefNameAttributeNameValidationHook", re);
          }
          try {
            GroupAttributeNameValidationHook.registerHookIfNecessary(true);
          } catch (RuntimeException re) {
            logBuiltInHookRegistrationError("GroupAttributeNameValidationHook", re);
          }
          try {
            StemAttributeNameValidationHook.registerHookIfNecessary(true);
          } catch (RuntimeException re) {
            logBuiltInHookRegistrationError("StemAttributeNameValidationHook", re);
          }
          try {
            GroupTypeTupleIncludeExcludeHook.registerHookIfNecessary(false);
          } catch (RuntimeException re) {
            logBuiltInHookRegistrationError("GroupTypeTupleIncludeExcludeHook", re);
          }
          try {
            GroupTypeSecurityHook.registerHookIfNecessary(false);
          } catch (RuntimeException re) {
            logBuiltInHookRegistrationError("GroupTypeSecurityHook", re);
          }
          try {
            GrouperAttributeAssignValueRulesConfigHook.registerHookIfNecessary(true);
          } catch (RuntimeException re) {
            logBuiltInHookRegistrationError("GrouperAttributeAssignValueRulesConfigHook", re);
          }
          try {
            GroupDoNotDeleteIfProvisionable.registerHookIfNecessary();
          } catch (RuntimeException re) {
            logBuiltInHookRegistrationError("GroupDoNotDeleteIfProvisionable", re);
          }{code}

and replace the registerBuiltInHook helper with a plain logging method:

{code:java}  /**
   * log a built in hook that failed to register.  The caller keeps going so the hooks after it
   * in the registration sequence are not silently skipped for the life of the jvm
   * @param hookName simple name of the hook, for logging
   * @param re the exception the registration threw
   */
  private static void logBuiltInHookRegistrationError(String hookName, RuntimeException re) {
    LOG.error("Error registering built in hook: " + hookName
        + ".  This hook will not be active until the problem is fixed and grouper is restarted.  "
        + "The other built in hooks are still being registered.", re);
  }{code}

This is behaviour preserving, verified: same eight hooks in the same order with the same arguments, same eight name strings for logging, same RuntimeException catch, and the log message text is byte identical. One hook failing is still logged and the remaining hooks still register.

Please make the change on GROUPER_7_BRANCH where GRP-7178 landed, so it flows to v6 and v4 through the normal backport rather than diverging. It is currently unfixed on both branches.
