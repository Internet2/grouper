---
key: GRP-932
cloud_key: 
onprem_url: https://todos.internet2.edu/browse/GRP-932
type: Bug
status: Resolved
resolution: Fixed
priority: Minor
reporter: Chris Hyzer <mchyzer@example.com>
assignee: Chris Hyzer <mchyzer@example.com>
created: 2013-09-10T14:56:00.048+0000
updated: 2013-09-10T14:57:40.901+0000
resolved: 2013-09-10T14:56:48.741+0000
components: [WS]
fixVersions: [2.1.6]
labels: []
links: []
---

# GRP-932  getMemberships has problems getting stem privileges for a user

From:Tim Darby
Sent: Wednesday, September 04, 2013 2:58 PM
To: Chris Hyzer
Cc: grouper-users
Subject: Re: [grouper-users] REST interface performance

Yeah, I need an example because no matter what subject I query on, it returns:

<WsGetMembershipsResults>
<resultMetadata>
<resultCode>SUCCESS</resultCode>
<resultMessage>Found 0 results involving 0 groups and 0 subjects</resultMessage>
<success>T</success>
</resultMetadata>
<responseMetadata>
<resultWarnings/>
<millis>21</millis>
<serverVersion>2.1.5</serverVersion>
</responseMetadata>
</WsGetMembershipsResults>

And this is the query I'm using:

https://grouper.arizona.edu/grouper-ws/servicesRest/xml/v2_1_005/subjects/123456789/memberships?wsLiteObjectType=WsRestGetMembershipsLiteRequest&actAsSubjectId=GrouperSystem&fieldName=creators


Tim Darby
The University of Arizona
Mosaic, Systems Integration and Architecture
UITS, Rm 335, 

On Mon, Sep 2, 2013 at 8:12 PM, Chris Hyzer <mchyzer@example.com> wrote:
I haven’t tried it, but in theory you should be able to call the getMemberships operation, passing in the subject, and the field is “creators” I believe, and it should return a bunch of memberships.  Get the stems in each of those and it is your answer.  Does it work, or do you want me to make an example?
 
Thanks,
Chris
 
From: On Behalf Of Tim Darby
Sent: Thursday, August 29, 2013 3:08 PM
To: Chris Hyzer

Subject: Re: [grouper-users] REST interface performance
 
Hi, I upgraded to 2.1.5, but I'm afraid I can't figure out how to construct a REST query that gives me what I want, namely all stems that a particular subject has create privilege on.


## Comments

### mchyzer - 2013-09-10T14:56:48.772+0000

This patch is required: 

GrouperServiceLogic.getMemberships() 

FROM: 

      if (wsStemLookup != null) { 
        wsStemLookup.retrieveStemIfNeeded(session, true); 
        stem = wsStemLookup.retrieveStem(); 

      } 
       
      //if filtering by stem, and stem not found, then dont find any memberships 
      if ((wsStemLookup == null || stem != null) && membersOk && groupsOk && stemsOk && attributeDefsOk) { 
        Set<Source> sources = GrouperUtil.convertSources(sourceIds); 


TO: 

      if (wsStemLookup != null && !wsStemLookup.blank()) { 
        wsStemLookup.retrieveStemIfNeeded(session, true); 
        stem = wsStemLookup.retrieveStem(); 

      } 
       
      //if filtering by stem, and stem not found, then dont find any memberships 
      if ((wsStemLookup == null || wsStemLookup.blank() || stem != null) && membersOk && groupsOk && stemsOk && attributeDefsOk) { 
        Set<Source> sources = GrouperUtil.convertSources(sourceIds); 



############### AND ############# 

GrouperServiceLogic.getMembershipsLite() 

FROM: 
    if (StringUtils.isNotBlank(stemName) || StringUtils.isNotBlank(stemUuid)) { 
      wsOwnerStemLookup = new WsStemLookup(stemName, stemUuid); 
    } 

TO: 
    if (StringUtils.isNotBlank(ownerStemName) || StringUtils.isNotBlank(ownerStemUuid)) { 
      wsOwnerStemLookup = new WsStemLookup(ownerStemName, ownerStemUuid); 
    } 
