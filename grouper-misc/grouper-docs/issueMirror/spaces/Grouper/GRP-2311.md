---
key: GRP-2311
cloud_key: 
onprem_url: https://todos.internet2.edu/browse/GRP-2311
type: Improvement
status: Resolved
resolution: Fixed
priority: Minor
reporter: Bert Bee-Lindgren <bert.bee-lindgren@example.com>
assignee: Bert Bee-Lindgren <bert.bee-lindgren@example.com>
created: 2019-09-04T16:35:36.690+0000
updated: 2021-02-25T00:02:28.001+0000
resolved: 2021-02-25T00:02:27.934+0000
components: [grouperLoader]
fixVersions: [2.5.44]
labels: []
links: []
---

# GRP-2311  Grouper Loader and SQL FetchSize

By default, Oracle's jdbc driver makes very small fetches of data as ResultSets are fetched. This led to incredibly slow performance reading larger loader queries at Georgia Tech where it took hours to cursor through a 3M row result across our AWS link. (The problem of these small fetches are worst with network latency)

The solution to this is JDBC's Statement's setFetchSize(N) method (defaults to 10, at least with Oracle).

While this is a general problem for Grouper's different connections to databases, this is mostly a problem for Loading and, probably, Subject Sources and not the Grouper Database because Grouper has to have very little network latency to its main database.

Grouper should have a db.<dbname>.fetchSize property that it then uses for its database queries. There might be a hibernate property for cases when Grouper has too much latency to its database.

## Comments

### Bert Bee-Lindgren - 2019-09-04T16:58:18.290+0000

{{{color:#000080}Here is a code snippet from line 435 GrouperLoaderResultset that sets the fetchSize.{color}}}

 

{{{color:#000080}try {color}{}}
{{ connection = grouperLoaderDb.connection();}}
{{ {color:#000080}try {color}{}}
{{ {color:#808080}// create and execute a SELECT
{color} statement = connection.createStatement();}}

{{ {color:#000080}int {color}fetchSize = GrouperLoaderConfig.retrieveConfig().propertyValueInt(String.format({color:#008000}"db.%s.fetchSize"{color}, dbName), {color:#0000ff}10{color});}}

{{ statement.setFetchSize(fetchSize);}}

{{ resultSet = statement.executeQuery(query);}}

### Bert Bee-Lindgren - 2019-09-04T17:00:06.792+0000

Note: This works for Oracle, but a more general default would probably be -1 and not set the FetchSize if it is not specified in properties.

### Chris Hyzer - 2021-02-25T00:02:27.996+0000

Im just going to set this to 1000 since i dont know why it would need to be tweaked...