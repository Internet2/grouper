---
key: GRP-856
cloud_key: 
onprem_url: https://todos.internet2.edu/browse/GRP-856
type: Improvement
status: Resolved
resolution: Fixed
priority: Minor
reporter: Chris Hyzer <mchyzer@example.com>
assignee: Chris Hyzer <mchyzer@example.com>
created: 2012-10-16T18:38:47.499+0000
updated: 2012-10-16T18:44:48.069+0000
resolved: 2012-10-16T18:41:33.299+0000
components: [WS]
fixVersions: [2.1.3]
labels: []
links: []
---

# GRP-856  allow local entities to login to WS without the folder prefix

the local entity subject api requires a prefix on the subject identifier, but basic auth cant have colons in the folder name...

## Comments

### mchyzer - 2012-10-16T18:41:33.330+0000

Configure this in the grouper-ws.properties:

# prepend this to the logged in user id to help this get resolved by the subject API
# you probably need to do this for local entities and WS logins
ws.security.prependToUserIdForSubjectLookup = etc:wsusers:

All subject ids authenticating to Grouper WS must be in that folder...

### mchyzer - 2012-10-16T18:44:48.069+0000

To patch 2.1.2

Change GrouperServiceJ2ee.java:

FROM:

      sourceId = StringUtils.trimToNull(GrouperWsConfig
          .getPropertyString(GrouperWsConfig.WS_LOGGED_IN_SUBJECT_DEFAULT_SOURCE));
    }

    //puts it in the log4j ndc context so userid is logged
    if (NDC.getDepth() == 0) {
      StringBuilder ndcBuilder = new StringBuilder("< ");


TO:

      sourceId = StringUtils.trimToNull(GrouperWsConfig
          .getPropertyString(GrouperWsConfig.WS_LOGGED_IN_SUBJECT_DEFAULT_SOURCE));
    }

    String subjectIdPrefix = GrouperWsConfig
        .getPropertyString("ws.security.prependToUserIdForSubjectLookup");
    if (!StringUtils.isBlank(subjectIdPrefix)) {
      userIdLoggedIn = subjectIdPrefix + userIdLoggedIn;
    }
    
    //puts it in the log4j ndc context so userid is logged
    if (NDC.getDepth() == 0) {
      StringBuilder ndcBuilder = new StringBuilder("< ");
