---
key: GRP-2157
cloud_key: 
onprem_url: https://todos.internet2.edu/browse/GRP-2157
type: Improvement
status: Resolved
resolution: Fixed
priority: Minor
reporter: Chris Hyzer <mchyzer@example.com>
assignee: Chris Hyzer <mchyzer@example.com>
created: 2019-05-29T14:26:25.824+0000
updated: 2019-06-12T09:13:10.092+0000
resolved: 2019-06-12T09:13:10.064+0000
components: [UI]
fixVersions: [2.4.0.patch, 2.5.0, 2.4.1]
labels: []
links: []
---

# GRP-2157  add template for policy group

this can be the next generation "include/exclude" and "require group".  Will prompt for name, display name, description, and a "require group".  If you enter "wikiUser" as the group name, then it will prompt for:
 * Create group: whateverFolder:wikiUser?     (default checked)
 ** Create group: whateverFolder:wikiUser_allow? (default checked)
 *** Create group: whateverFolder:wikiUser_allow_adhoc?
 ** Create group: whateverFolder:wikiUser_deny? (default checked)

 * 
 ** 
 *** Add: ref:someRedButtonGroup as member of whateverFolder:wikiUser_deny? (default checked)
 [note, only displayed if configured in config file]
 *** Create group: whateverFolder:wikiUser_deny_adhoc?
 ** Create group: whateverFolder:wikiUser_requireInGroup?

 * 
 ** 
 *** Add: ref:someActiveGroup as member of whateverFolder:wikiUser_requireInGroup? (default checked)
 [note, only displayed if configured in config file]

So by default you get an allow and deny group to make your policy.  If you want ad hocs, you can check those checkboxes.  I dont think we need the traditional systemOfRecord group, since you would just add ref groups to the "allow" group.

The template would do the logic to make the composites etc.  The overall is a composite allow minus deny. 

If there is a require group, then there would be a _preRequireGroup helper group, which is the allow minus deny, and the overall is the intersection of the pre-require helper group, and the requireInGroup.  If there is no deny group, then that composite can be skipped.  i.e. based on the checkboxes, it will make the correct groups.

This would also work on existing groups, in that case it might:
 # Add existing group members to the "allow" group
 # Add existing non group members to the allow adhoc group (or the allow group if there is no adhoc group)
 # It could also convert from include/exclude to allow/deny.  In this case if the systemOfRecord group is a loader group, it would keep a systemOfRecord group and add to the allow group

 

 

## Comments

### Bill Thompson  (lafayette.edu) - 2019-05-29T15:24:28.469+0000

This looks fine, and we would use it as is.

We tend to take care of the "active user" situations at the ref group level. However, one can see how the _requireInGroup could be used as general mechanism to add additional constraints to policy. For instance, a training requirement could result in a _requireInGroup group membership.

We would definitely use the default deny group.

I'm on the fence about the adhoc construction. We would normally put those in the app-specific ref groups folder, and then add those directly to _allow. That way policy management is somewhat segregated from access management.

 

### Chris Hyzer - 2019-06-12T09:13:10.090+0000

[https://spaces.at.internet2.edu/display/Grouper/Grouper+new+template+wizard+-+policy+groups]