The authz-standard-api-cilent is an implementation of the AuthZ Standard API on the client side.
It can either be used as a Java library or a standalone command line program.

There are no runtime dependencies for the Authz Standard API Client.

It runs JSON or XML.

It includes a test harness to verify that an implementation of the Authz Standard API service
on the server side is correct.