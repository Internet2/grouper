<ehcache>

  <!-- Sets the path to the directory where cache .data files are created. -->
  <diskStore path="java.io.tmpdir"/>


  <!--
    Default Cache configuration. These will applied to caches
    programmatically created through the CacheManager.

    The following attributes are required for defaultCache:

    maxInMemory       - Sets the maximum number of objects that
                        will be created in memory
    eternal           - Sets whether elements are eternal. If
                        eternal,  timeouts are ignored and the
                        element is never expired.
    timeToIdleSeconds - Sets the time to idle for an element
                        before it expires. Is only used if the
                        element is not eternal.  Idle time is now -
                        last accessed time
    timeToLiveSeconds - Sets the time to live for an element
                        before it expires. Is only used if the
                        element is not eternal.  TTL is now -
                        creation time
    overflowToDisk    - Sets whether elements can overflow to
                        disk when the in-memory cache has reached
                        the maxInMemory limit.
  -->
  <defaultCache
    maxElementsInMemory="10000"
    eternal="false"
    timeToIdleSeconds="120"
    timeToLiveSeconds="120"
    overflowToDisk="true"
  />

  <!--
    Predefined caches.  Add your cache configuration settings here.
    If you do not have a configuration for your cache a WARNING
    will be issued when the CacheManager starts
  -->

  <!-- XXX I have no idea what is appropriate for this cache -->
  <cache name="edu.internet2.middleware.group.GrouperAttribute"
    maxElementsInMemory="10000"
    eternal="false"
    timeToIdleSeconds="300"
    timeToLiveSeconds="600"
    overflowToDisk="true"
  />

  <!-- Non-mutable table with few items -->
  <cache name="edu.internet2.middleware.grouper.GrouperField"
    maxElementsInMemory="100"
    eternal="true"
    timeToIdleSeconds="300"
    timeToLiveSeconds="600"
    overflowToDisk="true"
  />

  <!-- XXX I have no idea what is appropriate for this cache -->
  <cache name="edu.internet2.middleware.group.GrouperGroup"
    maxElementsInMemory="10000"
    eternal="false"
    timeToIdleSeconds="300"
    timeToLiveSeconds="600"
    overflowToDisk="true"
  />

  <!-- XXX I have no idea what is appropriate for this cache -->
  <cache name="edu.internet2.middleware.group.GrouperList"
    maxElementsInMemory="10000"
    eternal="false"
    timeToIdleSeconds="300"
    timeToLiveSeconds="600"
    overflowToDisk="true"
  />

  <!-- XXX I have no idea what is appropriate for this cache -->
  <cache name="edu.internet2.middleware.group.GrouperMember"
    maxElementsInMemory="10000"
    eternal="false"
    timeToIdleSeconds="300"
    timeToLiveSeconds="600"
    overflowToDisk="true"
  />

  <!-- XXX I have no idea what is appropriate for this cache -->
  <cache name="edu.internet2.middleware.group.GrouperSubjectAttribute"
    maxElementsInMemory="10000"
    eternal="false"
    timeToIdleSeconds="300"
    timeToLiveSeconds="600"
    overflowToDisk="true"
  />

  <!-- XXX I have no idea what is appropriate for this cache -->
  <cache name="edu.internet2.middleware.group.GrouperSchema"
    maxElementsInMemory="10000"
    eternal="false"
    timeToIdleSeconds="300"
    timeToLiveSeconds="600"
    overflowToDisk="true"
  />

  <!-- Non-mutable table with few items -->
  <cache name="edu.internet2.middleware.grouper.GrouperType"
    maxElementsInMemory="100"
    eternal="true"
    timeToIdleSeconds="300"
    timeToLiveSeconds="600"
    overflowToDisk="true"
  />

  <!-- Non-mutable table with few items -->
  <cache name="edu.internet2.middleware.grouper.GrouperTypeDef"
    maxElementsInMemory="100"
    eternal="true"
    timeToIdleSeconds="300"
    timeToLiveSeconds="600"
    overflowToDisk="true"
  />

  <!-- XXX I have no idea what is appropriate for this cache -->
  <cache name="edu.internet2.middleware.group.SubjectImpl"
    maxElementsInMemory="10000"
    eternal="false"
    timeToIdleSeconds="300"
    timeToLiveSeconds="600"
    overflowToDisk="true"
  />

  <!-- Non-mutable table with few items -->
  <cache name="edu.internet2.middleware.grouper.SubjectTypeImpl"
    maxElementsInMemory="100"
    eternal="true"
    timeToIdleSeconds="300"
    timeToLiveSeconds="600"
    overflowToDisk="true"
  />

  <!-- XXX I have no idea what is appropriate for this cache -->
  <cache name="net.sf.hibernate.cache.QueryCache"
    maxElementsInMemory="10000"
    eternal="false"
    timeToIdleSeconds="300"
    timeToLiveSeconds="600"
    overflowToDisk="true"
  />

  <!-- XXX I have no idea what is appropriate for this cache -->
  <cache name="net.sf.hibernate.cache.StandardQueryCache"
    maxElementsInMemory="10000"
    eternal="false"
    timeToIdleSeconds="300"
    timeToLiveSeconds="600"
    overflowToDisk="true"
  />

  <!-- XXX I have no idea what is appropriate for this cache -->
  <cache name="net.sf.hibernate.cache.UpdateTimestampsCache"
    maxElementsInMemory="10000"
    eternal="false"
    timeToIdleSeconds="300"
    timeToLiveSeconds="600"
    overflowToDisk="true"
  />

</ehcache>
